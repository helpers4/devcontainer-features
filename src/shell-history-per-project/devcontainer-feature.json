{
  "version": "1.0.2",
  "id": "shell-history-per-project",
  "name": "Shell History Per Project",
  "description": "Persist shell history per project by automatically detecting and configuring all available shells (zsh, bash, fish). Supports auto-detection or manual shell selection.",
  "documentationURL": "https://github.com/helpers4/devcontainer/tree/main/src/shell-history-per-project",
  "options": {
    "shell": {
      "type": "string",
      "enum": [
        "auto",
        "zsh",
        "bash",
        "fish"
      ],
      "default": "auto",
      "description": "Shell type to configure history persistence for. 'auto' detects and configures all available shells."
    },
    "historyDirectory": {
      "type": "string",
      "default": "/workspaces/.shell-history",
      "description": "Directory where shell history will be persisted"
    },
    "maxHistorySize": {
      "type": "string",
      "default": "10000",
      "description": "Maximum number of history entries to keep"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}